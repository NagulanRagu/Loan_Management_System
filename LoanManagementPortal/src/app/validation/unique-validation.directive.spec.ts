import { UniqueValidationDirective } from './unique-validation.directive';

describe('UniqueValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueValidationDirective();
    expect(directive).toBeTruthy();
  });
});
