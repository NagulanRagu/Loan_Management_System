import { UniqueUsernameValidationDirective } from './unique-username-validation.directive';

describe('UniqueUsernameValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueUsernameValidationDirective();
    expect(directive).toBeTruthy();
  });
});
