import { UniqueEmailValidationDirective } from './unique-email-validation.directive';

describe('UniqueEmailValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueEmailValidationDirective();
    expect(directive).toBeTruthy();
  });
});
